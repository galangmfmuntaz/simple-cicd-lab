version: '3.8'

services:
  app:
    image: simple-cicd-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    container_name: flask-test
    restart: always
